# Production access code — set this in Vercel Environment Variables instead
# of committing a real value here.
# Vercel dashboard → Project → Settings → Environment Variables → add VITE_ACCESS_CODE
VITE_ACCESS_CODE=icare2026
